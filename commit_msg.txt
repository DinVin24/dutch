feat(visuals): replace placeholder table with custom 3D model

Replaces the SubResource BoxMesh table in game_board_3d.tscn with the
new custom table.glb model from assets/models. Cleaned up unused
table-specific sub-resources.

Agent: Executor (Antigravity)
