# MP-3 Disconnect / Rage Quit — Probe Report

Generated by `debug_mp_suite.gd` phase MP-3 (headless).

## Test scenario

Simulated `NetworkManager.player_disconnected(2)` mid-match while FSM active.

## Observed behavior (current code)

| Check | Result |
|-------|--------|
| FSM state changed | **No** — remains in current state |
| `peer_to_idx` updated | **No** — disconnected peer remains mapped |
| `game_board_3d` handler | **None** — does not connect `player_disconnected` |
| Lobby handler | Only `server_disconnected` on client returns to lobby |

## Manual V8 procedure

1. Run `.debug/run_mp_dual.sh` or two Lobby instances
2. Start 2-player match, skip Initial Peek
3. Client draws (pending visible)
4. Force-kill client process
5. Screenshot Host (V8): note FSM HUD, turn indicator, ghost pending on host

## Expected today (documented gap)

- Host may remain stuck waiting for disconnected player
- No bot backfill, no turn timeout, no `leave_game` from pause menu mid-match

## Recommended fix (backlog)

Wire in `game_board_3d.gd` or `game_manager.gd`:

1. Listen to `NetworkManager.player_disconnected`
2. Mark seat eliminated or replace with bot
3. Clear pending card if disconnected player held turn
4. Advance FSM via `next_turn()` or forced `end_turn`
